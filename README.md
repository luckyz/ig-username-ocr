# Instagram Username OCR

Detects Instagram Username OCR from a screenshot

## Quickstart

Install dependencies:

```
pip install -r requirements.txt
```

Then, execute the app:

```
python main.py
```
